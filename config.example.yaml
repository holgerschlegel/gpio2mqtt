### parameter related to the mqtt server
mqtt:
  host: <mqqt_host>
  # port: 1883
  # base_topic: gpio2mqtt
  user: <mqtt_user>
  password: <mqtt_password>
  # client_id:

### list of devices
devices:
  - id: <unique id>
    name: <unique name>
    type: <PulseCounter|ElectricityPulseMeter>
    gpio_pin: <pin>
    active_high: <true|false>
    # init_mode: <new|mqtt>
    # publish_interval_seconds: 0
    # pulses_per_kwh: <pulse_per_kwz>
